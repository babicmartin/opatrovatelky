# Snapshot Tests

Use this folder for tests that compare stable generated output against approved fixtures.

Current candidates:

- Rendered presenter HTML fragments.
- JSON payload shapes.
- PDF template text or metadata snapshots.

Snapshot updates must be explicit so real regressions are not accepted accidentally.
