# Smoke Tests

Use this folder for fast, high-level checks that prove the application can boot and key pages can render.

Smoke tests should stay small, deterministic, and suitable for the default PHPUnit run.
