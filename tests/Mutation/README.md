# Mutation Tests

Use this folder for mutation testing configuration, generated reports, and targeted mutation test notes.

Mutation testing should not run in the default test command. Add a dedicated runner, for example Infection, once the PHPUnit suite is stable enough to make mutation results useful.
